import { Text, View } from "react-native";
import { globalStyles } from "../../globalStyles";

export default function Sobre() {
  return (
    <View style={globalStyles.container}>
      <Text style={globalStyles.text}>Página Sobre</Text>
    </View>
  );
}