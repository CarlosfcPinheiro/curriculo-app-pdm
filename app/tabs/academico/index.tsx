import { Text, View } from "react-native";
import { globalStyles } from "../../globalStyles";

export default function Academico() {
  return (
    <View style={globalStyles.container}>
      <Text style={globalStyles.text}>Página experiência acadêmica</Text>
    </View>
  );
}